#include <iostream>

using namespace std;

int main() {

    int grade;
    string letterGrade;
    cout << "Enter the numeric score";
    cin >> grade;
    if (grade > 97) {
        letterGrade = "A++";
    } else if (grade > 89) {
        letterGrade = "A";
    } else if (grade )
    {
        /* code */
    }
    cout << letterGrade;

}