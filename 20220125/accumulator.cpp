#include <iostream>

using namespace std;


int main() {
    int number, total = 0;
    cout << "enter a number";
    cin >> number;

    while (number != -999) { //sentinal value

    }
}